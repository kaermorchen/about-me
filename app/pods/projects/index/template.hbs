<h1 class="mb-4">Portfolio</h1>

Filters:

<button
  class="btn btn-sm mb-3 {{if (eq selectedTags.length 0) "btn-primary" "btn-light"}} cursor-pointer"
  onclick={{action selectAll}}
>
  All
</button>

{{#each tags as |tag|}}
  <button
    class="btn btn-sm mb-3 {{if tag.selected "btn-primary" "btn-light"}} cursor-pointer"
    onclick={{toggle-action "selected" tag}}
  >
    {{tag.name}}
  </button>
{{/each}}

<div class="row">
  {{#each filteredProjects as |item|}}
    {{project-preview project=item class="col-4 mb-3"}}
  {{/each}}
</div>
