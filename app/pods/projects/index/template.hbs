<h1 class="mb-4">Portfolio</h1>

<button
  class="btn mb-3 {{unless selectedTags.length "btn-primary"}} cursor-pointer"
  onclick={{action selectAll}}
>
  All
</button>

{{#each tags as |tag|}}
  <button
    class="btn mb-3 {{if tag.selected "btn-primary"}} cursor-pointer"
    onclick={{action toggleTagSelected tag}}
  >
    {{tag.name}}
  </button>
{{/each}}

<div class="row">
  {{#each filteredProjects as |item|}}
    {{project-preview project=item class="col-md-6 col-lg-4 mb-3"}}
  {{/each}}
</div>
